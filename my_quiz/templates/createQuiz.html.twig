{% extends 'base.html.twig' %}

{% block title %}Creer votre "quiz"{% endblock %}

{% block body %}
    <div class="w-4/5 m-auto text-center">
        <div class="py-12 border-b border-gray-200">
            <h1 class="text-6xl pt-12 pb-8">
                Créez votre propre "Quiz"
            </h1>

            {{ form_start(form) }}
            {{ form_widget(form) }}
           <div>
                    <button type="submit" class="uppercase bg-blue-500 text-gray-100 text-lg font-semibold py-3 px-8 rounded-lg hover:bg-blue-600 transition duration-300">OK</button>
                </div>
            
            {{ form_end(form) }}

        </div>
    </div>
{% endblock %}